services:
  - type: web
    name: fjtcm-public-notice-mobile
    env: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: python scrape_jwc.py
